package server;


// SERVER SIDE USER ADMIN CONTROLS
public class UserAdmin {

    public UserAdmin() {
        // Constructor
    }


    //TODO: FIX ME! I'M JUST A TEST FOR NOW...
    public static String addUser() {
        return "Pass: User Created";
    }

    //TODO: FIX ME! I'M JUST A TEST FOR NOW...
    public static String logout(String sessionToken) {
        //ArrayList<String> validSessionTokens = new ArrayList<>(); // Initialisation
        //validSessionTokens.add(sessionToken, creationTime); // Login
        //validSessionTokens.remove(sessionToken); // Expire the session token

        // if validSessionTokens.contains(sessionToken) {
        return "Pass: Logout Successful";
    }
}
